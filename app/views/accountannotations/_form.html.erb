<%= form_with(model: accountannotation) do |form| %>
  <% if accountannotation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accountannotation.errors.count, "error") %> prohibited this accountannotation from being saved:</h2>

      <ul>
        <% accountannotation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :annotation_id %>
    <%= form.text_field :annotation_id %>
  </div>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.text_field :account_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
